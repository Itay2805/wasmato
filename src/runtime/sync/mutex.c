#include "mutex.h"

#include "lib/assert.h"

void mutex_lock_slow(mutex_t* mutex) {
    ASSERT(!"TODO: mutex_lock_slow");
}

void mutex_unlock_slow(mutex_t* mutex) {
    ASSERT(!"TODO: mutex_unlock_slow");
}

